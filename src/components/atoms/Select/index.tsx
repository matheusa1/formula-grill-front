import { Input } from '../Input'
import { SelectMain } from './parts/MainSelect'

export const Select = {
  Root: Input.Root,
  Label: Input.Label,
  Select: SelectMain,
  Feedback: Input.Feedback,
}
